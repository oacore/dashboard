import navigation from './navigation.yml'
import plugins from './plugins'
import issues from './issues'
import letters from './letters'

export default {
  issues,
  navigation,
  plugins,
  letters,
}

export { issues, navigation, plugins }
