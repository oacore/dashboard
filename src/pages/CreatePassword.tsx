import React from 'react';
import {PasswordUpdateFeature} from '@features/Authorization/PasswordUpdate.tsx';

export const CreatePasswordPage: React.FC = () => {

    return (
       <PasswordUpdateFeature/>
    );
};
